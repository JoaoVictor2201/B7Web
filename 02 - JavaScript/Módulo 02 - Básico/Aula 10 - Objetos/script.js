let personagem = {
    nome : 'João',
    idade : 23,
    pais: 'Brasil',
    atributos : {
        força : 20,
        mana : 100,
        inteligencia : 60
    }
}

console.log(`${personagem.nome} tem ${personagem.idade} anos e tem ${personagem.atributos.mana} pontos de mana`);