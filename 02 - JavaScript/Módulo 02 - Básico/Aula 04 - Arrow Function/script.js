function Somar(x, y) { /** Jeito padrão de criar funções */
    return x + y;
}

const SomarArrow = (x, y) => { /** Arrow Function */
    return x + y;
}

console.log(Somar(10, 5));
console.log(SomarArrow(2, 3));